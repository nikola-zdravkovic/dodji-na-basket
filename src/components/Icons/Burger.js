import { SvgIcon } from "@mui/material";
import BurgerIcon from "@public/images/icons/burger.svg";

const Burger = (props) => {
    return <SvgIcon {...props} component={BurgerIcon} />;
};

export default Burger;
