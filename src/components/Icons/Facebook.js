import { SvgIcon } from "@mui/material";
import FacebookIcon from "@public/images/icons/facebook.svg";

const Facebook = (props) => {
    return <SvgIcon {...props} component={FacebookIcon} />;
};

export default Facebook;
