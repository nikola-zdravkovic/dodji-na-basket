import { SvgIcon } from "@mui/material";
import InstagramIcon from "@public/images/icons/instagram.svg";

const Instagram = (props) => {
    return <SvgIcon {...props} component={InstagramIcon} />;
};

export default Instagram;
